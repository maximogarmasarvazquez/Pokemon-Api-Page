

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['raw.githubusercontent.com'], // Agrega el dominio de las imágenes externas
  },
};

export default nextConfig;
